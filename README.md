# Spam_SMS_Classification
This model uses machine learning to automatically categorize texts as "spam" or "ham" (legitimate) by learning patterns from a labeled dataset, employing techniques like text vectorization and algorithms to filter unwanted messages, focusing on features like word frequency and message structure for high accuracy, precision, recall, and F1-score. 
